<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright 2008, 2009, 2010, 2011, 2015 by the Department of Computer 
	Science in the University of Oxford; see lgpl-3.0.txt and gpl.txt for details -->
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<prerequisites>
		<maven>3.0.5</maven>
	</prerequisites>

	<parent>
		<groupId>org.sonatype.oss</groupId>
		<artifactId>oss-parent</artifactId>
		<version>7</version>
	</parent>

	<!-- USAGE: `mvn compile` compiles the main sources, `mvn test` runs the tests,
		`mvn package` builds the HermiT bundle and supporting artifacts, and
		`mvn install` installs them in the local Maven repository. -->

	<groupId>edu.stanford.protege</groupId>
	<artifactId>hermit-reasoner</artifactId>
	<version>1.5.0-SNAPSHOT</version>
	<packaging>bundle</packaging>

	<name>HermiT</name>
	<url>http://hermit-reasoner.com/</url>
	<description>HermiT is an OWL 2 reasoner that can determine whether an ontology is consistent, classify classes and properties, and realize individuals.
	HermiT was originally developed by Boris Motik, Birte Glimm, Rob Shearer, Giorgos Stoilos, and Ian Horrocks at the Department of Computer Science of the University of Oxford.
	This repository is maintained by the Protégé project to provide HermiT builds for current OWL API and Protégé releases.
	This package includes the JAutomata library (http://jautomata.sourceforge.net/) and builds it directly because that project is no longer actively maintained.</description>

	<licenses>
		<license>
			<name>LGPL</name>
			<url>http://www.gnu.org/licenses/lgpl-3.0.txt</url>
		</license>
	</licenses>

	<distributionManagement>
		<snapshotRepository>
			<id>ossrh</id>
			<url>https://central.sonatype.com/repository/maven-snapshots/</url>
		</snapshotRepository>
		<repository>
			<id>ossrh</id>
			<url>https://central.sonatype.com</url>
		</repository>
	</distributionManagement>

	<properties>
		<!-- General info. -->
		<homepage>http://hermit-reasoner.com/</homepage>
		<updatefile>https://raw.githubusercontent.com/protegeproject/hermit-reasoner/main/update.properties</updatefile>
		<mainClass>org.semanticweb.HermiT.cli.CommandLine</mainClass>
		<!-- structure of output files -->
		<reportsDirectory>${project.build.directory}/junit-reports</reportsDirectory>
		<!-- To generate a debug version, use `mvn -DdebugHermiT=true` when running 
			Maven. -->
		<debugHermiT>false</debugHermiT>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!-- Keep the default build on a published release until Protege 6 artifacts are available. -->
		<protege.version>5.6.6</protege.version>
	</properties>

	<profiles>
		<profile>
			<id>protege6</id>
			<!-- Activate after installing the current Protege 6 build locally. -->
			<properties>
				<protege.version>6.0.0-alpha-1-SNAPSHOT</protege.version>
			</properties>
		</profile>
		<profile>
			<id>release</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.apache.maven.plugins</groupId>
						<artifactId>maven-gpg-plugin</artifactId>
						<version>3.2.7</version>
						<configuration>
							<skip>${release.signing.disabled}</skip>
						</configuration>
						<executions>
							<execution>
								<id>sign-artifacts</id>
								<phase>verify</phase>
								<goals>
									<goal>sign</goal>
								</goals>
							</execution>
						</executions>
					</plugin>
					<plugin>
						<groupId>org.sonatype.central</groupId>
						<artifactId>central-publishing-maven-plugin</artifactId>
						<version>0.9.0</version>
						<extensions>true</extensions>
						<configuration>
							<publishingServerId>ossrh</publishingServerId>
							<autoPublish>true</autoPublish>
							<waitUntil>published</waitUntil>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.6.0</version>
				<configuration>
					<source>1.8</source>
					<target>1.8</target>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<version>2.19.1</version>
				<configuration>
					<excludes>
						<exclude>**/BlockingValidatorTest.java</exclude>
					</excludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.0.2</version>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-source-plugin</artifactId>
				<version>3.0.1</version>
				<executions>
					<execution>
						<id>attach-source</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.0.1</version>
				<configuration>
					<show>public</show>
				</configuration>
				<executions>
					<execution>
						<id>attach-javadocs</id>
						<goals>
							<goal>jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<version>3.2.0</version>
				<extensions>true</extensions>
				<configuration>
					<instructions>
						<Main-Class>org.semanticweb.HermiT.cli.CommandLine</Main-Class>
						<Implementation-Title>${project.name}</Implementation-Title>
						<Implementation-Vendor>Protégé project (originally developed by the HermiT team at the University of Oxford)</Implementation-Vendor>
						<Implementation-Version>${project.version}.${maven.build.timestamp}</Implementation-Version>
						<Build-Date>${timestamp}</Build-Date>
						<Bundle-Name>HermiT Reasoner</Bundle-Name>
						<Bundle-Category>protege</Bundle-Category>
						<Bundle-Description>An OWL 2 reasoner</Bundle-Description>
						<Bundle-DocURL>${homepage}</Bundle-DocURL>
						<Bundle-SymbolicName>org.semanticweb.hermit;singleton:=true</Bundle-SymbolicName>
						<Bundle-Vendor>Protégé project (originally developed by Boris Motik, Birte Glimm, Rob Shearer, Giorgos Stoilos, and Ian Horrocks at the University of Oxford)</Bundle-Vendor>
						<Bundle-Version>${project.version}</Bundle-Version>
						<Update-Url>${updatefile}</Update-Url>
						<Embed-Transitive>true</Embed-Transitive>
						<Embed-Dependency>
							automaton,
							commons-logging,
							axiom-api,
							axiom-c14n,
							axiom-impl,
							axiom-dom,
							hppcrt,
							commons-cli
						</Embed-Dependency>
						<Export-Package>
							org.semanticweb.HermiT.*,
							rationals.*
						</Export-Package>
						<Import-Package>
							!org.apache.*,
							!gnu.getopt,
							!javax.*,
							javax.xml.stream.*,
							!org.codehaus.*,
							!org.jaxen*,
							org.protege.editor.owl.*;version="[6,7)",
							*
						</Import-Package>
					</instructions>
				</configuration>
				<executions>
					<execution>
						<id>bundle-manifest</id>
						<phase>install</phase>
						<goals>
							<goal>manifest</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<dependencies>
		<dependency>
			<groupId>commons-logging</groupId>
			<artifactId>commons-logging</artifactId>
			<version>1.1.3</version>
		</dependency>
		<dependency>
			<groupId>net.sourceforge.owlapi</groupId>
			<artifactId>owlapi-distribution</artifactId>
			<version>5.5.1</version>
		</dependency>
		<dependency>
			<groupId>com.github.vsonnier</groupId>
			<artifactId>hppcrt</artifactId>
			<version>0.7.5</version>
		</dependency>
		<dependency>
			<groupId>org.apache.ws.commons.axiom</groupId>
			<artifactId>axiom-api</artifactId>
			<version>1.2.14</version>
		</dependency>
		<dependency>
			<groupId>org.apache.ws.commons.axiom</groupId>
			<artifactId>axiom-c14n</artifactId>
			<version>1.2.14</version>
		</dependency>
		<dependency>
			<groupId>org.apache.ws.commons.axiom</groupId>
			<artifactId>axiom-impl</artifactId>
			<version>1.2.14</version>
		</dependency>
		<dependency>
			<groupId>org.apache.ws.commons.axiom</groupId>
			<artifactId>axiom-dom</artifactId>
			<version>1.2.14</version>
		</dependency>
		<dependency>
			<groupId>dk.brics.automaton</groupId>
			<artifactId>automaton</artifactId>
			<version>1.11-8</version>
		</dependency>
		<dependency>
			<groupId>junit</groupId>
			<artifactId>junit</artifactId>
			<version>4.12</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>edu.stanford.protege</groupId>
			<artifactId>protege-editor-owl</artifactId>
			<version>${protege.version}</version>
			<optional>true</optional>
			<exclusions>
				<exclusion>
					<groupId>org.testng</groupId>
					<artifactId>testng</artifactId>
				</exclusion>
				<exclusion>
					<artifactId>protege-owlapi-extensions</artifactId>
					<groupId>edu.stanford.protege</groupId>
				</exclusion>
				<exclusion>
					<artifactId>org.protege.xmlcatalog</artifactId>
					<groupId>edu.stanford.protege</groupId>
				</exclusion>
				<exclusion>
					<artifactId>owlapi-distribution</artifactId>
					<groupId>net.sourceforge.owlapi</groupId>
				</exclusion>
			</exclusions>
		</dependency>
		<dependency>
			<groupId>commons-cli</groupId>
			<artifactId>commons-cli</artifactId>
			<version>1.4</version>
		</dependency>
	</dependencies>

	<developers>
		<developer>
			<id>bmotik</id>
			<name>Boris Motik</name>
			<email>boris.motik@cs.ox.ac.uk</email>
		</developer>
		<developer>
			<id>bglimm</id>
			<name>Birte Glimm</name>
			<email>birte.glimm@uni-ulm.de</email>
		</developer>
		<developer>
			<id>ignazio1977</id>
			<name>Ignazio Palmisano</name>
			<email>ignazio1977@users.sourceforge.net</email>
		</developer>
		<developer>
			<id>phillord</id>
			<name>Phillip Lord</name>
			<email>phillip.lord@newcastle.ac.uk</email>
		</developer>
	</developers>


	<mailingLists>
		<mailingList>
			<name>Hermit Users Group</name>
			<archive>http://groups.google.com/group/hermit-users</archive>
		</mailingList>
	</mailingLists>


	<scm>
		<connection>scm:git:https://github.com/protegeproject/hermit-reasoner.git</connection>
		<developerConnection>scm:git:ssh://git@github.com/protegeproject/hermit-reasoner.git</developerConnection>
		<url>https://github.com/protegeproject/hermit-reasoner</url>
	</scm>

</project>
